<Window
    x:Class="PL.Courier.CourierListWindow" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:fa="http://schemas.fontawesome.io/icons/"
    xmlns:local="clr-namespace:PL.Courier" 
    mc:Ignorable="d" 
    Title="Courier List" Height="640" Width="800" 
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}" 
    Loaded="Window_Loaded" Closed="Window_Closed"
    Background="#FFF0F7FF">

    <Window.Resources>
        <DropShadowEffect x:Key="ZDepth1" BlurRadius="8" ShadowDepth="2" Color="#AABBCC" Opacity="0.4"/>

        <Style TargetType="ComboBox">
            
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#ADD8E6"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border CornerRadius="12" 
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="30" />
                                    </Grid.ColumnDefinitions>

                                    <ContentPresenter Margin="10,0,0,0" 
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      VerticalAlignment="Center" 
                                                      IsHitTestVisible="False"/>

                                    <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0" Fill="#005A9C" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False"/>

                                    <ToggleButton Grid.ColumnSpan="2" Background="Transparent" BorderThickness="0" 
                                                  IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>
                                </Grid>
                            </Border>

                            <Popup AllowsTransparency="true" 
       IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" 
       Placement="Bottom" 
       PopupAnimation="Slide">
                                <Border Background="White" BorderBrush="#ADD8E6" BorderThickness="1" CornerRadius="8"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" 
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#ADD8E6"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Effect" Value="{StaticResource ZDepth1}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="15" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Effect" Value="{StaticResource ZDepth1}"/>
            <Setter Property="Background" Value="{Binding VehicleType, Converter={StaticResource VehicleRowColorConverterKey}}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border CornerRadius="12" Background="{TemplateBinding Background}"
                                BorderBrush="#DDEEFF" BorderThickness="1" Margin="10,2">
                            <DataGridCellsPresenter ItemsPanel="{TemplateBinding ItemsPanel}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="25,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Couriers:" Grid.Column="0" FontSize="42" FontWeight="Black" 
                       VerticalAlignment="Center" Foreground="#FFB1B7E8"/>

            <ComboBox SelectedValue="{Binding Path=Courier, Mode=TwoWay}" 
                      ItemsSource="{Binding Source={StaticResource CouriersCollectionKey}}" 
                      Grid.Column="2" Margin="10,0" VerticalAlignment="Center"
                      SelectionChanged="ComboBox_SortCourierProperty">

                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource ConvertEnumToWordsKey}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <ComboBox SelectedValue="{Binding SelectedActiveFilter, Mode=TwoWay}" 
                      SelectedValuePath="Tag" Grid.Column="3" VerticalAlignment="Center"
                      SelectionChanged="ActiveFilter_SelectionChanged">
                <ComboBoxItem Content="All" Tag="{x:Null}" />
                <ComboBoxItem Content="Active" Tag="True" />
                <ComboBoxItem Content="Inactive" Tag="False" />
            </ComboBox>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding CourierList}" 
                  SelectedItem="{Binding SelectedCourier}" 
                  MouseDoubleClick="CourierList_MouseDoubleClick" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  GridLinesVisibility="None" Margin="10" 
                  Background="Transparent" BorderThickness="0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Salary" Binding="{Binding Salary}" Width="Auto"/>
                <DataGridTemplateColumn Header="Active" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding Active}" IsEnabled="False" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Order Type" Binding="{Binding VehicleType}" Width="Auto"/>
                <DataGridTextColumn Header="On-Time" Binding="{Binding NumberOfOrdersDeliveredOnTime}" Width="Auto"/>
                <DataGridTextColumn Header="Late" Binding="{Binding NumberOfOrdersDeliveredLate}" Width="Auto"/>
                <DataGridTextColumn Header="Current Order" Binding="{Binding NumberOfCurrentOrderId}" Width="*"/>
                <DataGridTemplateColumn Header="Action" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="DeleteBtn_Click" 
        Height="25" 
        Width="80" 
        FontSize="10" 
        Background="#FFF5F5" 
        BorderBrush="#FFDADA" 
        Effect="{x:Null}" 
        Visibility="{Binding Converter={StaticResource ConvertCourierCanBeDeletedToVisibility}}">

                                <StackPanel Orientation="Horizontal">
                                    <fa:ImageAwesome Icon="Trash" 
                         Foreground="Red" 
                         Height="12" 
                         Margin="0,0,5,0" />

                                    <TextBlock Text="Delete" 
                   VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Add New Courier" Grid.Row="2" 
                HorizontalAlignment="Center" Height="45" Width="220" 
                Margin="0,10,0,20" Click="AddBtn_Click" FontSize="15"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,15">
            <fa:ImageAwesome Icon="Motorcycle" 
                     Height="30" 
                     Margin="0,0,10,0"
                     VerticalAlignment="Center">
                <fa:ImageAwesome.Foreground>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Black"/>
                        <GradientStop Color="#A855F7" Offset="1"/>
                    </LinearGradientBrush>
                </fa:ImageAwesome.Foreground>
            </fa:ImageAwesome>

            <TextBlock Text="MediRoute" 
               FontSize="30" 
               FontWeight="Bold" 
               VerticalAlignment="Center">
                <TextBlock.Foreground>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Black"/>
                        <GradientStop Color="#A855F7" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>