<Window x:Class="PL.Courier.CourierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local="clr-namespace:PL.Courier"
        mc:Ignorable="d"
        Title="Courier Details" Height="700" Width="600" 
        Background="#FFF8F0FF"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Closed="Window_Closed">

    <Window.Resources>
        <Color x:Key="MainPurpleColor">#A855F7</Color>
        <SolidColorBrush x:Key="MainPurpleBrush" Color="#A855F7"/>
        <DropShadowEffect x:Key="ZDepth1" BlurRadius="8" ShadowDepth="2" Color="#BBAACC" Opacity="0.4"/>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#A855F7"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>

        <Style x:Key="ValidatedTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1C4E9"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="BoxBorder" CornerRadius="12" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource ValidatedTextBox}"/>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1C4E9"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border CornerRadius="12" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>
                                    <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0" Stroke="#A855F7" StrokeThickness="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    <ToggleButton Grid.ColumnSpan="2" Background="Transparent" BorderThickness="0" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True">
                                <Border Background="White" BorderBrush="#D1C4E9" BorderThickness="1" CornerRadius="8" MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1C4E9"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Foreground" Value="#6A1B9A"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Effect" Value="{StaticResource ZDepth1}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" CornerRadius="15" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#F3E5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Courier Information" FontSize="36" FontWeight="Black" 
                   Foreground="#A855F7" Margin="35,25,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="230"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Content="ID:"/>
                <TextBox Grid.Row="0" Grid.Column="1" 
                         Text="{Binding CurrentCourier.Id, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                         IsReadOnly="{Binding ButtonText, Converter={StaticResource ConvertUpdateToTrueKey}}">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource ValidatedTextBox}">
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding CurrentCourier.Id, Converter={StaticResource IdToErrorConverterKey}}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsFocused}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="BorderBrush" Value="Red"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Label Grid.Row="1" Content="Name:"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CurrentCourier.Name}"/>

                <Label Grid.Row="2" Content="Phone Number:"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CurrentCourier.PhoneNumber, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource ValidatedTextBox}">
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding CurrentCourier.PhoneNumber, Converter={StaticResource PhoneToErrorConverterKey}}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsFocused}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="BorderBrush" Value="Red"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Label Grid.Row="3" Content="Email:"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CurrentCourier.Email, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource ValidatedTextBox}">
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding CurrentCourier.Email, Converter={StaticResource EmailToErrorConverterKey}}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsFocused}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="BorderBrush" Value="Red"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Label Grid.Row="4" Content="Password:"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CurrentCourier.Password, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource ValidatedTextBox}">
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding CurrentCourier.Password, Converter={StaticResource PasswordToErrorconverterKey}}" Value="True"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsFocused}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="BorderBrush" Value="Red"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Label Grid.Row="5" Content="Salary:"/>
                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CurrentCourier.Salary}" />

                <Label Grid.Row="6" Content="Active:"/>
                <CheckBox Grid.Row="6" Grid.Column="1" IsChecked="{Binding CurrentCourier.Active}" Content="Active Status" VerticalAlignment="Center"/>

                <Label Grid.Row="7" Content="Max Distance:"/>
                <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding CurrentCourier.MaxPersonalDistance}"/>

                <Label Grid.Row="8" Content="Vehicle:"/>
                <ComboBox Grid.Row="8" Grid.Column="1" ItemsSource="{Binding Source={StaticResource VehicleCollectionKey}}" SelectedItem="{Binding CurrentCourier.Vehicle}"/>

                <Label Grid.Row="9" Content="Employment Date:"/>
                <DatePicker Grid.Row="9" Grid.Column="1" SelectedDate="{Binding CurrentCourier.EmploymentDate}" IsEnabled="False" Width="220" HorizontalAlignment="Left"/>

                <Label Grid.Row="10" Content="Orders on time:"/>
                <TextBox Grid.Row="10" Grid.Column="1" Text="{Binding CurrentCourier.NumOfProvidedOnTime}" IsReadOnly="True" Width="80"/>

                <Label Grid.Row="11" Content="Orders late:"/>
                <TextBox Grid.Row="11" Grid.Column="1" Text="{Binding CurrentCourier.NumOfProvidedLate}" IsReadOnly="True" Width="80"/>

                <Label Grid.Row="12" Content="Existing Order:" Visibility="{Binding CurrentCourier.ExistingOrder, Converter={StaticResource OrderExistsToVisibilityConverterKey}}" />
                <StackPanel Grid.Row="12" Grid.Column="1" Orientation="Horizontal">
                    <TextBox Text="{Binding CurrentCourier.ExistingOrder.OrderId}" IsReadOnly="True" Width="150" Visibility="{Binding CurrentCourier.ExistingOrder, Converter={StaticResource OrderExistsToVisibilityConverterKey}}"/>
                    <Button Height="30" Width="85" Margin="10,0,0,0" Click="ViewBtn_click" Background="#A855F7" Visibility="{Binding CurrentCourier.ExistingOrder, Converter={StaticResource OrderExistsToVisibilityConverterKey}}">
                        <StackPanel Orientation="Horizontal">
                            <fa:ImageAwesome Icon="Eye" Foreground="White" Height="14" Margin="0,0,8,0" />
                            <TextBlock Text="View" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <Border Grid.Row="2" Padding="0,20" Background="#FFF8F0FF">
            <Button Content="{Binding ButtonText}" Width="180" Height="45" Click="BtnAddUpdate_Click" HorizontalAlignment="Center"/>
        </Border>

        <TextBlock Grid.Row="3" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15">
            <TextBlock.Foreground>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black"/>
                    <GradientStop Color="#A855F7" Offset="1"/>
                </LinearGradientBrush>
            </TextBlock.Foreground>
            <InlineUIContainer>
                <fa:ImageAwesome Icon="Motorcycle" Height="26" Margin="0,0,10,0" VerticalAlignment="Center" 
                                 Foreground="{Binding RelativeSource={RelativeSource AncestorType=TextBlock}, Path=Foreground}"/>
            </InlineUIContainer>
            <Run Text="MediRoute"/>
        </TextBlock>
    </Grid>
</Window>