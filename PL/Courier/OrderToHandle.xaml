<Window x:Class="PL.Courier.OrderToHandle"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:fa="http://schemas.fontawesome.io/icons/"

        Title="MediRoute - Selection" Height="720" Width="1100"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Background="#FFF5F0FF" 
        Loaded="Window_Loaded"
        Closed="Window_Closed">

    <Window.Resources>
        <Color x:Key="MainPurpleColor">#A855F7</Color>
        <SolidColorBrush x:Key="MainPurpleBrush" Color="{StaticResource MainPurpleColor}"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#10B981"/>

        <DropShadowEffect x:Key="ZDepth1" BlurRadius="8" ShadowDepth="2" Color="#D1C4E9" Opacity="0.4"/>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1C4E9"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder" CornerRadius="12" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" 
                                              Content="{TemplateBinding SelectionBoxItem}"/>

                                    <Path x:Name="ArrowPath" Grid.Column="1" Data="M 0 0 L 4 4 L 8 0" 
                                  Stroke="#A855F7" StrokeThickness="2" 
                                  VerticalAlignment="Center" HorizontalAlignment="Center"/>

                                    <ToggleButton Grid.ColumnSpan="2" Background="Transparent" BorderThickness="0" 
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True">
                                <Border Background="White" BorderBrush="#D1C4E9" BorderThickness="1" CornerRadius="8" MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="MainBorder" Property="Background" Value="#F3F4F6"/>
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#D1D5DB"/>
                                <Setter TargetName="ArrowPath" Property="Stroke" Value="#9CA3AF"/>
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="ColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource MainPurpleBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,0,0,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="10,6"/>
            <Setter Property="Height" Value="55"/>
            <Setter Property="Effect" Value="{StaticResource ZDepth1}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border CornerRadius="15" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="#E9D5FF" 
                                BorderThickness="1">
                            <DataGridCellsPresenter ItemsPanel="{TemplateBinding ItemsPanel}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FAF5FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="180"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="CollectButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SuccessGreen}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Margin="10,0,10,20">
            <TextBlock Grid.Column="0" Text="Select Order to Handle" 
               FontSize="36" 
               FontWeight="Black" 
               Foreground="{StaticResource MainPurpleBrush}" 
               VerticalAlignment="Center"
               HorizontalAlignment="Left"/>

            <fa:ImageAwesome Icon="Motorcycle" Grid.Column="1" 
                     Height="30" 
                     Foreground="{StaticResource MainPurpleBrush}"
                     HorizontalAlignment="Center" 
                     VerticalAlignment="Center" 
                     Visibility="{Binding OpenOrders, Converter={StaticResource visibilityToLoadedIconKey}}"
          
                     RenderTransformOrigin="0.5,0.5">
                <fa:ImageAwesome.RenderTransform>
                    <RotateTransform />
                    </fa:ImageAwesome.RenderTransform>
                    <fa:ImageAwesome.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                         From="0" To="360" 
                                         Duration="0:0:1.5" 
                                         RepeatBehavior="Forever" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </fa:ImageAwesome.Triggers>
            </fa:ImageAwesome>
        </Grid>



        <Border Grid.Row="1" Background="White" CornerRadius="15" Margin="10,0,10,25" Padding="15" Effect="{StaticResource ZDepth1}">
            <StackPanel Orientation="Horizontal" FlowDirection="LeftToRight">
                <TextBlock Text="Sort by:" FontSize="20" Foreground="{StaticResource MainPurpleBrush}" FontWeight="Medium"></TextBlock>
                <ComboBox ItemsSource="{Binding FilterOpenOrderList}" 
                          Margin="10,0"
                          SelectedItem="{Binding OrderSort}"
                          SelectionChanged=" OrderSort_SelectionChanged"  IsEnabled="{Binding OpenOrders, Converter={StaticResource enabledToLoadedconvertorKey}}" />
                <TextBlock Text="Filter by:" FontSize="20" Foreground="{StaticResource MainPurpleBrush}" FontWeight="Medium"></TextBlock>

                <ComboBox ItemsSource="{Binding Source={StaticResource OrderTypeCollectionKey}}" IsEnabled="{Binding OpenOrders, Converter={StaticResource enabledToLoadedconvertorKey}}"
                          Margin="10,0" 
                          SelectedItem="{Binding  OrderFilter}"
                          SelectionChanged="OrderFilter_SelectionChanged"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding OpenOrders}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  CanUserAddRows="False"
                  HeadersVisibility="Column" 
                  Background="Transparent" 
                  BorderThickness="0"
                  GridLinesVisibility="None"
                  ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}"
                  SelectedItem="{Binding SelectedOrder}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="2*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Distance" Binding="{Binding ActualDistance}" Width="2*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Type" Binding="{Binding OrderType}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Volume" Binding="{Binding Volume}" Width="*" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Fragile">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding Fragile, UpdateSourceTrigger=PropertyChanged}" IsEnabled="False"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
               

                <DataGridTemplateColumn Header="Action" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource CollectButtonStyle}" 
        Click="CollectBtn_Click"
        Height="32" 
        Width="100">
                                <StackPanel Orientation="Horizontal">
                                    <fa:ImageAwesome Icon="HandPointerOutline" 
                         Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
                         Height="14" 
                         Margin="0,0,8,0" />
                                    <TextBlock Text="Collect" 
                   VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" FontSize="24" FontWeight="Bold" 
           HorizontalAlignment="Right" Margin="0,10,10,0" 
           Foreground="{StaticResource MainPurpleBrush}" Opacity="0.7">
    <InlineUIContainer>
        <fa:ImageAwesome Icon="Motorcycle" 
                         Height="20" 
                         Margin="0,0,8,0" 
                         VerticalAlignment="Center" 
                         Foreground="{Binding RelativeSource={RelativeSource AncestorType=TextBlock}, Path=Foreground}"/>
    </InlineUIContainer>
    <Run Text="MediRoute"/>
        </TextBlock>
    </Grid>
</Window>