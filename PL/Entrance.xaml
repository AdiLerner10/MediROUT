<Window x:Class="PL.Entrance"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Entrance" Height="550" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="LilacBrush" Color="#B685FF"/>
        <SolidColorBrush x:Key="DarkPurpleBrush" Color="#6A1B9A"/>

        <Style x:Key="RoundedInputStyle" TargetType="Control">
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border CornerRadius="22" Background="White" BorderThickness="1.5" BorderBrush="Transparent">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="12" Color="#D1C4E9" Direction="270" ShadowDepth="3" Opacity="0.5"/>
                            </Border.Effect>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="15,0" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource LilacBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DarkPurpleBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="22" Background="{TemplateBinding Background}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="4" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#E0F7FA" Offset="0"/>
                <GradientStop Color="#F3E5F5" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border MaxWidth="380" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="20">
            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:1"/>
                            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,50,0,-50" To="0" Duration="0:0:0.8"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>

            <StackPanel Height="416" Width="339">
                <TextBlock Text="SYSTEM LOGIN" FontSize="30" FontWeight="Black" 
                            Foreground="#FFB657E4" HorizontalAlignment="Center" Margin="0,0,0,35">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.2"/>
                    </TextBlock.Effect>
                </TextBlock>

                <TextBlock Text="User Id" Margin="15,0,0,5" Foreground="#555" FontWeight="SemiBold"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" 
                         Text="{Binding UserId,UpdateSourceTrigger=PropertyChanged}"
                         Foreground="{Binding UserId, Converter={StaticResource IdValidationColorConverter}}"
                         MaxLength="9"
                         Margin="0,0,0,20"/>

                <TextBlock Text="Password" Margin="15,0,0,5" Foreground="#555" FontWeight="SemiBold"/>
                <Grid Margin="0,0,0,35">
                    <PasswordBox
                        local:PasswordHelper.Attach="True" 
             local:PasswordHelper.BoundPassword="{Binding Password, Mode=TwoWay}">
                        <PasswordBox.Style>
                            <Style TargetType="PasswordBox" BasedOn="{StaticResource RoundedInputStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </PasswordBox.Style>
                    </PasswordBox>

                    <TextBox Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource RoundedInputStyle}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>



                    <ToggleButton IsChecked="{Binding IsPasswordVisible}" 
                                  HorizontalAlignment="Right" VerticalAlignment="Center" 
                                  Margin="0,0,15,0" Background="Transparent" BorderBrush="Transparent" Cursor="Hand">
                        <TextBlock Text="👁" FontSize="20" Foreground="{StaticResource LilacBrush}"/>
                    </ToggleButton>
                </Grid>

                <Button Content="LOGIN" Style="{StaticResource ModernButtonStyle}" Height="50" 
                        Command="{Binding LoginCommand}" Foreground="White" Background="#FFB657E4" Click="btnLogin_Click" IsDefault="True"/>

                <TextBlock Grid.Row="3" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center">
                    <TextBlock.Foreground>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#6D28D9"/>
                            <GradientStop Color="#A855F7" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
<InlineUIContainer>
    <fa:ImageAwesome Icon="Motorcycle" 
                     Height="20" 
                     Margin="0,0,8,0" 
                     VerticalAlignment="Center" 
                     Foreground="{Binding RelativeSource={RelativeSource AncestorType=TextBlock}, Path=Foreground}"/>
</InlineUIContainer>
<Run Text="MediRoute"/>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Window>