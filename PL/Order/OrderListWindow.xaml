<Window x:Class="PL.Order.OrderListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local1="clr-namespace:PL.Order"
        xmlns:local2="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Order List" Height="640" Width="1000"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="Window_Loaded" Closed="Window_Closed"
        Background="#FFF0F7FF"
        Language="he-IL">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#005A9C"/>
        <SolidColorBrush x:Key="LightBlue" Color="#E1F5FE"/>
        <SolidColorBrush x:Key="BorderBlue" Color="#ADD8E6"/>
        <DropShadowEffect x:Key="ZDepth1" BlurRadius="8" ShadowDepth="2" Color="#AABBCC" Opacity="0.4"/>

        <DataTemplate x:Key="ColumnCellTemplate">
            <Border BorderBrush="#20000000" BorderThickness="0,0,1,0" Margin="0,2,10,2" Padding="5,5,15,5">
                <TextBlock Text="{Binding}" VerticalAlignment="Center" FontWeight="Medium"/>
            </Border>
        </DataTemplate>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBlue}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="contentPresenter" Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="{TemplateBinding Padding}" VerticalAlignment="Center" IsHitTestVisible="False"/>
                                    <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Stroke="{StaticResource PrimaryBlue}" 
                                          StrokeThickness="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ToggleButton x:Name="toggleButton" Background="Transparent" BorderThickness="0" 
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Popup x:Name="PART_Popup" AllowsTransparency="true" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                                   Placement="Bottom" PopupAnimation="Slide">
                                <Border x:Name="dropDownBorder" Background="White" BorderBrush="{StaticResource BorderBlue}" 
                                        BorderThickness="1" CornerRadius="8" MinWidth="{TemplateBinding ActualWidth}" 
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}" Effect="{StaticResource ZDepth1}">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                <Setter TargetName="border" Property="Background" Value="{StaticResource LightBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OrderListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="Background" Value="{Binding OrderType, Converter={StaticResource RowColorConverter}}"/>
            <Setter Property="Margin" Value="0,3,0,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="#DCDCDC" BorderThickness="1">
                            <GridViewRowPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StyledButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBlue}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="12" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#CC4444"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" BorderBrush="#40CC4444" BorderThickness="1" CornerRadius="8" Padding="8,2" Background="#FFF5F5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterTextBoxPlaceholderStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="10" Background="White" BorderBrush="{StaticResource BorderBlue}" BorderThickness="1.5">
                            <Grid>
                                <TextBlock x:Name="Placeholder" Text="{TemplateBinding Tag}" Foreground="Gray" Margin="10,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                                <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="10,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                    <Condition Property="IsFocused" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Orders" FontSize="44" FontWeight="Black" VerticalAlignment="Center" Margin="20,0" Foreground="#FFB1B7E8"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Top">
                <TextBlock Text="Filter by:" FontSize="14" FontWeight="Bold" Foreground="#6B7280" Margin="5,0"/>
                <ComboBox ItemsSource="{Binding FilterOrderList}" 
                          SelectedItem="{Binding OrderFilter, Mode=TwoWay}" SelectionChanged="filterCmb_SelectionChanged" Width="250" HorizontalAlignment="Left">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource ConvertEnumToWordsKey}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Grid Margin="0,5">
                    <ComboBox ItemsSource="{Binding OrderFilter, Converter={StaticResource FilterCollectionConverter}}" 
                              Visibility="{Binding OrderFilter, Converter={StaticResource ComboVissibilityConverter}}" 
                              SelectedItem="{Binding FilterSelectedItem, Mode=TwoWay}" Width="250" HorizontalAlignment="Left"
                              SelectionChanged="FilterOrdersChoose_SelectionChanged" >
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource ConvertEnumToWordsKey}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <StackPanel Orientation="Horizontal" Visibility="{Binding OrderFilter, Converter={StaticResource TextBlockVisibilityConverter}}">
                        <TextBox Style="{StaticResource FilterTextBoxPlaceholderStyle}" Tag="{Binding OrderFilter, Converter={StaticResource FilterChooseToStringConverter}}" 
                                 Width="180" Height="35" Text="{Binding FilterSelectedItem, Mode=OneWayToSource}"/>
                        <Button Content="filter" Style="{StaticResource StyledButton}" Width="60" Height="35" Margin="10,0" Click="btnSortConfirmation_Click" />
                    </StackPanel>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Column="2" VerticalAlignment="Top">
                <TextBlock Text="Sort by:" FontSize="14" FontWeight="Bold" Foreground="#6B7280" Margin="5,0"/>
                <ComboBox ItemsSource="{Binding Source={StaticResource OrdersCollectionKey}}" 
                          SelectedItem="{Binding OrderSort, Mode=TwoWay}" SelectionChanged="filterCmb_SelectionChanged" Width="180" HorizontalAlignment="Left">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource ConvertEnumToWordsKey}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </Grid>

        <ListView Grid.Row="1" ItemsSource="{Binding OrderList}" SelectedItem="{Binding SelectedOrder}"
                  ItemContainerStyle="{StaticResource OrderListViewItemStyle}" MouseDoubleClick="DataGrid_MouseDoubleClick"
                  Background="Transparent" BorderThickness="0" Margin="20,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" Width="70" DisplayMemberBinding="{Binding OrderId}"/>
                    <GridViewColumn Header="Type" Width="120" DisplayMemberBinding="{Binding OrderType}"/>
                    <GridViewColumn Header="Direction" Width="90" DisplayMemberBinding="{Binding InOrOut}"/>
                    <GridViewColumn Header="Distance" Width="90" DisplayMemberBinding="{Binding AirDistance}"/>
                    <GridViewColumn Header="Remaining" Width="140">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar Value="{Binding RemainingTime, Converter={StaticResource RemainingTimeColorConverer}}" 
                                             Foreground="{Binding RemainingTime, Converter={StaticResource RemainingTimeColorValueConverer}}" Width="100" Height="10" Background="White"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Status" Width="120" DisplayMemberBinding="{Binding ScheduleStatus}"/>
                    <GridViewColumn Header="Action" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Click="btnCancleOrder_Click" 
        Height="25" 
        Width="85" 
        FontSize="10" 
        Background="#FFF5F5" 
        BorderBrush="#FFDADA" 
        Effect="{x:Null}" 
        Visibility="{Binding OrderId, Converter={StaticResource VissibilityToCancelConverter}}">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="1" 
                    CornerRadius="12">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <StackPanel Orientation="Horizontal">
                                        <fa:ImageAwesome Icon="Times" 
                         Foreground="Red" 
                         Height="12" 
                         Margin="0,0,5,0" />
                                        <TextBlock Text="Cancel" 
                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Grid.Row="2" Content="Create New Order" Style="{StaticResource StyledButton}" Width="200" Height="45" Click="btnAddOrder_Click" />

        <TextBlock Grid.Row="3" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center">
            <TextBlock.Foreground>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#6D28D9"/>
                    <GradientStop Color="#A855F7" Offset="1"/>
                </LinearGradientBrush>
            </TextBlock.Foreground>
    <InlineUIContainer>
        <fa:ImageAwesome Icon="Motorcycle" 
                         Height="20" 
                         Margin="0,0,8,0" 
                         VerticalAlignment="Center" 
                         Foreground="{Binding RelativeSource={RelativeSource AncestorType=TextBlock}, Path=Foreground}"/>
    </InlineUIContainer>
    <Run Text="MediRoute"/>
        </TextBlock>
    </Grid>
</Window>